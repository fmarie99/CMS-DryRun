// create an interface for contact that has string properties firstname, lastname, physical address, and delivery address
export interface Contact {
  firstName: string;
  lastName: string;
  physicalAddress: string;
  deliveryAddress: string;
}
